
## 问题

## Data
更新文档，检查

## Now
- @Src/ECS/System/FeatureSystem @Src/ECS/System/TestSystem @Src/ECS/System/AbilitySystem 
UI跟逻辑分离
测试系统需要检查有没有问题，深度思考，技能系统没有按照我的想法去做，我是想着自由添加删除技能，左边是所有技能分类；右边显示当前选中单位当前有的技能，也按照分类显示，可右键禁用技能，点击左侧栏就可以添加技能，点击右侧栏就可以移除技能
这个功能很常见，当前并没有实现，实现起来并不难
深度思考详细分析广泛搜索，大量搜索网上资料，使用context,github等MCP搜索相关资料

- []【严重问题】Dash技能我把攻击力设置成1000，这个关系应该不大，主要是伤害敌人的数量有十几个就卡？


## 测试系统
@Src/ECS/System/AbilitySystem @Data/DataKey/Attribute 
我需要做测试系统，在Src\ECS\System下面新建文件夹，用于游戏运行中的测试，包括属性、技能等功能
- 测试系统需要做成通用，后面还要加很多测试功能，增加起来要方便，深度思考怎么做
- 增加鼠标选择Entity功能，在游戏当中，鼠标点击能够选择一个Entity，这个也暂时放在测试系统，用于测试系统选择entity
- 用一个按钮（测试）点击，在左上角显示，然后有下拉列表，选择对应的测试功能，，比如属性测试功能就能设置属性，比如攻击、生命、是否无敌等属性
- 技能测试，自由添加删除技能，左边是所有技能分类；右边是当前有的技能，可以右键禁用技能
我可能表达不全，
深度思考详细分析


## TODO
- [x] 技能逻辑复用，比如特效、伤害这种可以写成工具不用次次都重新，AbilityConfig.cs增加参数，比如伤害类型等
- [] 伤害是选择工具遍历entity距离，还是用碰撞事件？特效、武器、子弹都要一般都要用碰撞。
- [] 检查技能逻辑、测试系统
- [] AbilityConfig.cs顺序
- []回旋镖运动很有问题
- **要不要用行为树状态机来管理运动系统，比如运动结束时自动切换成优先级高的一种状态？不对，运动跟状态是两个概念，比如眩晕的时候可以运动**
- **更新运动系统所有说明文档，尤其是一大堆<c></c>感觉不好，深度思考**

- Entity销毁时哪些关系要跟着销毁，比如角色销毁，技能、物品、武器、子弹、特效要跟着销毁
- Windsurf自动补全失效
- SKILL优化，老是不触发

## 移动系统
- Godot Tween是否能替代我现在的运动策略，为什么
  

## 伤害
- 碰撞后一次伤害/多次伤害，伤害间隔

## TodoList



- [] 详细查看数字飘字逻辑

- [] DataKey检查，分多过多的分多一个文件
- [] 事件检查

- 表格里面不能填具体伤害，比如我想填技能伤害=防御力*3+10，这个应该怎么做？或者说这个应该算逻辑不是数据？



## 大方向
- []特效系统
- []物品系统
- []BUFF系统
- []三选一玩法

## C#表格插件
```markdown
@addons/SlimeDataTable_CSharp @Data/Data @Tools/ResourceGenerator  
现在数据存放是在Data\Data路径，用config.cs代码作为模板，然后根据这个代码模板生成godot场景，但是.trres和C#不互通，就比如Dash.cs和DashConfig.tres，本来可以根据Ability.Movement.Dash完成分组，一个变量搞定，现在多了个FeatureGroup就是因为不互通，现在分成两个其实维护起来也不好，如果Dash技能的分组变了，Dash.cs和DashConfig.tres都要跟着变，如果都能获取C#或者数据直接用C#而不是.tres，就能一改就全改，问题在.tres不互通
GDS表格插件也有问题，不能用继承，比如enemyconfig继承unitconfig，但是enemyconfig不能重置unitconfig的属性，就是因为GDS表格插件有问题才想着要改
我以前也改过纯C#的方案，后面又改回.tres了，根本原因是godot引擎支持+可视化UI，一个是因为resources_spreadsheet_view插件现成的能够直接显示表格，不过挺僵硬的，没有excel那样好用，优点是.tres在godot中能够用检查器
我现在想改回纯C#，在Data\DataNew路径单独生成，迁移，不影响当前设置，纯C#而不是.tres。
- 难点1：与godot场景兼容问题，比如AuraShieldConfig.tres，.tres包含场景信息，而C#似乎不包含，其实传路径也可以，ResourcePaths.cs是Tools\ResourceGenerator工具生成的，自动更新，可以传路径，解决了不自动更新的问题，AbilityConfig.cs的EffectScene、ProjectileScene都是PackedScene，对比字符串路径的好处是能够在godot中拖拽，这个是否能兼容，如果不能，那我把文件拖拽进去要替换字符串路径才行，这样也可以，实际上字符串路径更好，不需要跟godot场景兼容，是最好的方式，最简单，也容易实现，游戏数据管理肯定不会说用个场景放进去的，这样更好
- 难点2：UI显示，之前用tres主要是因为能够使用Godot的UI，能够直接显示，但是Godot的UI本身就是一种限制，应该没有直接用.net生态做出来的软件UI更好。其他AI我不打算做这个项目，但你是GLM5.1，在长任务方面优势明显，你就算是写一个C#版的excel那样好用的东西我觉得是可行的，可以尝试写一个C#版的表格软件，完全兼容C#和Godot
- 主要抉择点：使用GodotUI还是完全不用Godot，我之前纯C#就是这么想的，效果还行，之前没这么做是感觉做个软件比较麻烦，现在可以尝试
- 要不要用ResourceGenerator作为更新路径的手段？但是这样的话有点奇怪，如果说要做软件的话，为什么还需要这个小工具？还是说在软件内部自动检查路径是否有问题，如果是空路径（一般直接移动路径就会出现）就在表格软件上直接显示出来
- 做表格软件需要做大量的搜索，网上会有很多轮子，不一定需要自己做，仔细搜索，同时也学习别人优秀的方案怎么实现的
深度思考详细分析广泛搜索，必须仔细查看godot文档和使用filesystem mcp查看godot源码(我clone到本地了：/mnt/e/Godot/GodotEngine/godot路径)，广泛查询网上
详细分析深度思考这种改法是否能够完美实现，
- 我肯定有很多地方表达不清晰，你在网上详细搜索相关资料仔细研究后跟我详细说明，不清晰的地方跟我交流
```

## 完成基本Demo后有待完善
- []AI系统检查
- AI新加的东西检查，TargetSelector检查
- []Data事件、DataKey的规范写到Skill
- []ComponentPreset用代码代替场景是否更好？相关的Component放在一起，比如位置目标技能的右摇杆移动组件应该跟技能相关组件放一起
- []检查Datakey和Datakeyregister

## 有待解决
- Data系统、伤害系统增伤易伤区分
- 护盾、Buff、Effect系统
- Docs\ECS\修改\有待修改.md

- Tween 动画做成工具？有Timer有必要用Tween吗？






