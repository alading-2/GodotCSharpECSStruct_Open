# 技能系统架构设计理念

**文档类型**：架构设计  
**目标受众**：架构师、新成员、AI 助手  
**最后更新**：2026-01-17

---

## 核心理念

> [!IMPORTANT]
> **技能系统完全遵循项目 ECS 框架原则**，不引入任何外部框架的独立类概念

**框架一致性**：

| ECS 原则 | 技能系统实现 |
|:---|:---|
| Scene 即 Entity | `AbilityEntity` 继承 `Node` 实现 `IEntity` |
| Data 唯一数据源 | 所有技能数据通过 `DataKey` 存储在 `Data` 中 |
| Component 无状态 | 组件只读写 `_data`，不持有私有状态 |
| EntityManager 入口 | 通过 `EntityManager.Spawn<AbilityEntity>()` 创建 |
| 事件驱动 | 使用 `entity.Events.Emit()` 通信 |

**设计借鉴**：

| 来源 | 借鉴理念 | 本地化实现 |
|:---|:---|:---|
| UE5 GAS | GameplayTags | 标签存储在 `Data` 中，由 `TagComponent` 处理 |
| Unity GAS | TargetCatcher | 作为 `TargetingComponent` 实现 |
| War3 | 事件+条件+动作 | 通过 `TriggerComponent` 监听事件 |

---

## 技能分类

| 特性 | 主动技能 | 被动技能 | 武器技能 |
|:---|:---|:---|:---|
| 触发 | 玩家输入 | 自动 | 自动攻击 |
| 充能 | ✅ | ❌ | ❌ |
| 冷却 | ✅ | ✅ 内部冷却 | ✅ 攻速 |
| 消耗 | ✅ | ❌ | ⚠️ 弹药 |

---

## 触发模式 - [Flags] 位运算

```csharp
[Flags]
public enum AbilityTriggerMode
{
    None = 0,
    Manual = 1 << 0,        // 手动
    OnEvent = 1 << 1,       // 事件
    Periodic = 1 << 2,      // 周期
    Permanent = 1 << 3,     // 永久
    Auto = 1 << 4,          // 自动
}
```

**使用**：
```csharp
var mode = (AbilityTriggerMode)_data.Get<int>(DataKey.AbilityTriggerMode);
if (mode.HasFlag(AbilityTriggerMode.OnEvent)) SubscribeToEvent();
if (mode.HasFlag(AbilityTriggerMode.Periodic)) StartPeriodicTimer();
```

---

## 目标系统 - 5 层 DataKey

> [!NOTE]
> **不使用独立类**，所有目标配置通过 `DataKey` 存储在 `Data` 中

### DataKey 定义

```csharp
// 目标系统
public const string AbilityTargetOrigin = "AbilityTargetOrigin";       // 选取原点
public const string AbilityTargetGeometry = "AbilityTargetGeometry";   // 几何形状
public const string AbilityTargetTeamFilter = "AbilityTargetTeamFilter"; // 阵营过滤
public const string AbilityTargetTypeFilter = "AbilityTargetTypeFilter"; // 类型过滤
public const string AbilityTargetSorting = "AbilityTargetSorting";     // 排序方式
public const string AbilityRange = "AbilityRange";                     // 范围
public const string AbilityMaxTargets = "AbilityMaxTargets";           // 最大目标数
```

### 枚举定义

```csharp
public enum AbilityTargetOrigin { Self, Unit, Point, EventSource, Cursor }
public enum AbilityTargetGeometry { Single, Circle, Box, Line, Cone, Chain, Global }
public enum AbilityTargetSorting { Nearest, Farthest, LowestHealth, Random }

[Flags]
public enum AbilityTargetTeamFilter { Friendly = 1, Enemy = 2, Neutral = 4, Self = 8 }

[Flags]
public enum AbilityTargetTypeFilter { Hero = 1, Creep = 2, Boss = 4, Ground = 8, Flying = 16 }
```

---

## 标签系统 - 通过 DataKey 实现

> [!WARNING]
> **不使用独立 `TagConfig` 类**，标签作为 `List<string>` 存储在 `Data` 中

### DataKey 定义

```csharp
// 标签系统
public const string AbilityAssetTags = "AbilityAssetTags";                       // 技能自身标签
public const string AbilityActivationRequiredTags = "AbilityActivationRequiredTags"; // 激活必须
public const string AbilityActivationBlockedTags = "AbilityActivationBlockedTags";   // 激活阻止
public const string AbilityBlockAbilitiesWithTags = "AbilityBlockAbilitiesWithTags"; // 阻止其他技能
public const string AbilityCancelAbilitiesWithTags = "AbilityCancelAbilitiesWithTags"; // 取消其他技能
```

### 配置示例

```csharp
{
    // 技能自身标签
    { DataKey.AbilityAssetTags, new List<string> { "Ability.Fire.Fireball" } },
    
    // 必须在地面才能释放
    { DataKey.AbilityActivationRequiredTags, new List<string> { "State.Grounded" } },
    
    // 眩晕时无法释放
    { DataKey.AbilityActivationBlockedTags, new List<string> { "State.Stunned" } },
}
```

### TagComponent 实现

```csharp
public partial class TagComponent : Node, IComponent
{
    private Data? _data;
    private IEntity? _entity;

    public void OnComponentRegistered(Node entity)
    {
        if (entity is IEntity iEntity)
        {
            _data = iEntity.Data;
            _entity = iEntity;
        }
    }

    /// <summary>检查标签激活条件</summary>
    public bool CheckActivationTags(IEntity owner)
    {
        if (_data == null) return true;
        
        var required = _data.Get<List<string>>(DataKey.AbilityActivationRequiredTags);
        var blocked = _data.Get<List<string>>(DataKey.AbilityActivationBlockedTags);
        
        // 检查必须拥有的标签
        if (required != null && required.Count > 0)
        {
            var ownerTags = owner.Data.Get<List<string>>(DataKey.EntityTags) ?? new();
            foreach (var tag in required)
            {
                if (!ownerTags.Contains(tag)) return false;
            }
        }
        
        // 检查不能拥有的标签
        if (blocked != null && blocked.Count > 0)
        {
            var ownerTags = owner.Data.Get<List<string>>(DataKey.EntityTags) ?? new();
            foreach (var tag in blocked)
            {
                if (ownerTags.Contains(tag)) return false;
            }
        }
        
        return true;
    }
}
```

---

## 组件职责矩阵

| 组件 | 职责 | 主动 | 被动 | 武器 |
|:---|:---|:---:|:---:|:---:|
| TriggerComponent | 触发模式处理 | ✅ | ✅ | ✅ |
| CooldownComponent | 冷却管理 | ✅ | ✅ | ✅ |
| ChargeComponent | 充能管理 | ✅ | ❌ | ❌ |
| CostComponent | 消耗检查 | ✅ | ❌ | ⚠️ |
| TargetingComponent | 目标选择 | ✅ | ✅ | ✅ |
| TagComponent | 标签检查 | ✅ | ✅ | ✅ |

---

## 激活检查链 - AbilitySystem 协调

> [!NOTE]
> **检查逻辑分散在各 Component**，由 `AbilitySystem` 协调调用

```csharp
// AbilitySystem.cs
public bool CanUseAbility(AbilityEntity ability)
{
    if (!ability.IsEnabled) return false;
    if (ability.IsActive) return false;

    // 1. 获取 ID
    var abilityId = ability.Data.Get<string>(DataKey.Id) ?? string.Empty;

    // 2. 查询 ENTITY_TO_ABILITY 关系的父级
    var ownerId = EntityRelationshipManager.GetParentEntitiesByChildAndType(
        abilityId, EntityRelationshipType.ENTITY_TO_ABILITY).FirstOrDefault();

    if (string.IsNullOrEmpty(ownerId)) return false;

    var owner = EntityManager.GetEntityById(ownerId) as IEntity;
    if (owner == null) return false;

    // 1. 标签检查 (TagComponent)
    var tag = EntityManager.GetComponent<TagComponent>(ability);
    if (tag != null && !tag.CheckActivationTags(owner))
        return false;

    // 2. 冷却检查 (CooldownComponent)
    var cooldown = EntityManager.GetComponent<CooldownComponent>(ability);
    if (cooldown != null && !cooldown.IsReady())
        return false;

    // 3. 充能检查 (ChargeComponent)
    var charge = EntityManager.GetComponent<ChargeComponent>(ability);
    if (charge != null && !charge.HasCharge())
        return false;

    // 4. 消耗检查 (CostComponent)
    var cost = EntityManager.GetComponent<CostComponent>(ability);
    if (cost != null && !cost.CanAfford(owner))
        return false;

    return true;
}
```

---

## 架构分层

```
AbilitySystem (协调器)
    ↓ 管理
AbilityEntity (技能实体)
    ├─ Data: 所有技能数据 (DataKey)
    ├─ Events: 技能事件总线
    └─ Components:
        ├─ TriggerComponent   → 触发逻辑
        ├─ CooldownComponent  → 冷却逻辑
        ├─ ChargeComponent    → 充能逻辑
        ├─ CostComponent      → 消耗逻辑
        ├─ TargetingComponent → 目标选择
        └─ TagComponent       → 标签检查
    ↓ 执行
AbilityEffect (效果处理) → DamageSystem, EntityManager 等
```

---

## 配置示例

### 反伤光环

```csharp
{
    { DataKey.Name, "thorns_aura" },
    { DataKey.AbilityType, (int)AbilityType.Passive },
    
    // 触发：事件 + 周期
    { DataKey.AbilityTriggerMode, (int)(AbilityTriggerMode.OnEvent | AbilityTriggerMode.Periodic) },
    { DataKey.AbilityTriggerEvent, GameEventType.Unit.Damaged },
    { DataKey.AbilityTriggerInterval, 0.1f },
    { DataKey.AbilityCooldown, 0.5f },
    
    // 目标
    { DataKey.AbilityTargetOrigin, (int)AbilityTargetOrigin.EventSource },
    { DataKey.AbilityTargetGeometry, (int)AbilityTargetGeometry.Single },
    { DataKey.AbilityTargetTeamFilter, (int)AbilityTargetTeamFilter.Enemy },
    
    { DataKey.AbilityDamage, 10f },
}
```

### 火焰风暴

```csharp
{
    { DataKey.Name, "flame_storm" },
    { DataKey.AbilityType, (int)AbilityType.Active },
    
    { DataKey.AbilityTriggerMode, (int)AbilityTriggerMode.Manual },
    { DataKey.AbilityCooldown, 8.0f },
    { DataKey.AbilityCostType, (int)AbilityCostType.Mana },
    { DataKey.AbilityCostAmount, 50f },
    
    // 标签：必须在地面
    { DataKey.AbilityActivationRequiredTags, new List<string> { "State.Grounded" } },
    { DataKey.AbilityActivationBlockedTags, new List<string> { "State.Stunned" } },
    
    // 目标
    { DataKey.AbilityTargetOrigin, (int)AbilityTargetOrigin.Point },
    { DataKey.AbilityTargetGeometry, (int)AbilityTargetGeometry.Circle },
    { DataKey.AbilityRange, 200f },
    { DataKey.AbilityTargetTeamFilter, (int)AbilityTargetTeamFilter.Enemy },
    { DataKey.AbilityMaxTargets, 99 },
    
    { DataKey.AbilityDamage, 80f },
}
```

---

## 与 ECS 框架一致性

| 框架原则 | 技能系统实现 | 状态 |
|:---|:---|:---:|
| Scene 即 Entity | `AbilityEntity : Node, IEntity` | ✅ |
| Data 唯一数据源 | 所有数据通过 `DataKey` 存储 | ✅ |
| Component 无状态 | 组件只读写 `_data` | ✅ |
| EntityManager 入口 | `EntityManager.Spawn<AbilityEntity>()` | ✅ |
| EntityRelationshipManager | 技能-拥有者关系管理 | ✅ |
| 事件驱动 | `Events.Emit()` 通信 | ✅ |
| 不引入外部类 | 无 `AbilitySpec`, `GameplayEffect` 等 | ✅ |

---

## 相关文档

- [Entity 架构设计理念](../Entity/Entity架构设计理念.md)
- [Data 容器系统](../../../../Src/ECS/Data/README.md)
- [Component 规范](../../../../Src/ECS/Component/Component规范.md)

---

**维护者**：项目团队  
**文档版本**：v2.1  
**创建日期**：2026-01-16  
**重构日期**：2026-01-17
