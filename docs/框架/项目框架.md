# Brotato 复刻项目 - Godot C# 伪 ECS 框架设计文档

## 📋 目录

1. [架构概览](#1-架构概览)
2. [已完成模块](#2-已完成模块)
3. [待实现核心系统](#3-待实现核心系统)
4. [目录结构](#4-目录结构)
5. [设计模式与最佳实践](#5-设计模式与最佳实践)
6. [开发路线图](#6-开发路线图)

---

## 1. 架构概览

### 1.1 核心理念

**目标游戏类型**: Survivor-like 割草游戏（类似《土豆兄弟》）
**核心挑战**: 同屏处理 500+ 动态实体（敌人、子弹、掉落物）

### 1.2 架构选型：Godot C# 伪 ECS 混合架构

**为什么不用传统 ECS 框架？**

- Godot 的 Node 系统已经提供了强大的组合能力
- 第三方 ECS 框架与 Godot 编辑器集成困难
- 伪 ECS 可以充分利用 Godot 的可视化编辑器

**三层架构设计**:

```
┌─────────────────────────────────────────────────────┐
│  逻辑层 (Logic Layer)                                │
│  - Node Composition (组件组合)                       │
│  - Component System (HealthComponent, etc.)         │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│  数据层 (Data Layer)                                 │
│  - 静态配置: Resource (.tres 文件)                   │
│  - 动态数据: Data 容器 (挂载到 Node)                 │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│  优化层 (Optimization Layer)                         │
│  - ObjectPool (对象池复用)                           │
│  - RenderingServer/PhysicsServer (高负载场景)       │
└─────────────────────────────────────────────────────┘
```

---

## 2. 已完成模块

### ✅ 2.1 Log (日志系统)

**路径**: `scenes/Tools/Logger/Log.cs`

**核心价值**:

- 统一的调试输出接口
- 分级过滤（Trace/Debug/Info/Success/Warning/Error）
- 条件编译优化（Release 版本零性能损耗）

**设计亮点**:

```csharp
// 实例化设计，每个类独立上下文
private static readonly Log Log = new Log("Player");

// 条件编译，DEBUG 模式外完全移除
[Conditional("DEBUG")]
public void Debug(object message) { }
```

**使用场景**:

- 开发阶段：追踪游戏逻辑流程
- 性能分析：记录关键操作耗时
- 错误排查：定位 Bug 来源

---

### ✅ 2.2 ObjectPool (对象池)

**路径**: `scenes/Tools/ObjectPool/ObjectPool.cs`

**核心价值**:

- **性能关键**: 避免频繁 GC，提升 30-50% 帧率
- 自动管理 Node 生命周期（ProcessMode、Visible）
- 全局 ObjectPoolManager 统一调度

**设计亮点**:

```csharp
// 泛型设计，支持任意类型
ObjectPool<Bullet> bulletPool;

// 自动归还，无需记住所属池
ObjectPoolManager.ReturnToPool(bullet);

// 统计信息，便于性能分析
PoolStats stats = pool.GetStats();
float hitRate = stats.HitRate;  // 命中率
```

**必须使用对象池的场景**:

- ✅ 子弹/投射物（生命周期短，数量大）
- ✅ 敌人（频繁生成和销毁）
- ✅ 掉落物（经验球、金币）
- ✅ 粒子特效节点
- ✅ 伤害数字 UI

---

### ✅ 2.3 Data (动态数据容器)

**路径**: `scenes/Tools/data/Data.cs`

**核心价值**:

- 实现 ECS 的 Component Data 概念
- 运行时灵活添加/修改属性
- 支持事件监听，实现响应式编程

**设计亮点**:

```csharp
// 强类型访问 + 类型转换
int hp = data.Get<int>("CurrentHp", 100);

// 算术运算，适合 Buff/Debuff 系统
data.Add("Damage", 10);      // 增加伤害
data.Multiply("Speed", 1.5f); // 加速 50%

// 事件监听，实现数据驱动 UI
data.On("CurrentHp", (oldVal, newVal) => {
    UpdateHealthBar();
});
```

**使用场景**:

- Buff/Debuff 系统（临时属性修改）
- 动态标记（IsBoss、IsInvincible）
- 跨组件数据共享
- 配置热更新

---

### ✅ 2.4 NodeExtensions (节点扩展)

**路径**: `scenes/Tools/extensions/NodeExtensions.cs`

**核心价值**:

- 为 Node 提供 Data 挂载能力
- 实现 Entity-Component 关联
- 自动生命周期管理（ConditionalWeakTable）

**设计亮点**:

```csharp
// 扩展方法，调用简洁
enemy.GetData().Set("Reward", 100);

// 弱引用，不阻止 GC
// Node 被回收时，Data 自动清理
```

**架构意义**:
这是伪 ECS 的核心粘合剂，让 Godot Node 具备了 ECS Entity 的数据挂载能力。

---

## 3. 待实现核心系统

### 🔲 3.1 Component System (组件系统)

**优先级**: ⭐⭐⭐⭐⭐ (最高)

**职责**: 提供可复用的功能模块，通过组合实现实体行为

#### 必需组件列表

**1. HealthComponent (生命值组件)**

```csharp
using System;

public partial class HealthComponent : Node
{
    [Export] public float MaxHp { get; set; } = 100;
    public float CurrentHp { get; private set; }

    // ✅ 使用 C# 原生 Event（推荐）
    public event Action<float>? Damaged;   // 参数：伤害值
    public event Action<float>? Healed;    // 参数：治疗量
    public event Action? Died;             // 无参数

    public override void _Ready()
    {
        CurrentHp = MaxHp;
    }

    public void TakeDamage(float damage)
    {
        if (CurrentHp <= 0) return;

        CurrentHp = Math.Max(0, CurrentHp - damage);
        Damaged?.Invoke(damage);

        if (CurrentHp <= 0)
        {
            Died?.Invoke();
        }
    }

    public void Heal(float amount)
    {
        if (CurrentHp >= MaxHp) return;

        CurrentHp = Math.Min(MaxHp, CurrentHp + amount);
        Healed?.Invoke(amount);
    }
}
```

**使用场景**: 玩家、敌人、可破坏物体

**为什么用 C# Event？**

- ✅ 性能更好（编译期优化）
- ✅ 类型安全（编译期检查）
- ✅ 不依赖 Godot 引擎（可用于纯 C# 类）
- ✅ 代码更简洁

**2. HitboxComponent (攻击判定)** - 继承 Area2D

```csharp
public partial class HitboxComponent : Area2D
{
    [Export] public float Damage { get; set; }
    [Export] public float Knockback { get; set; }
    [Export] public uint CollisionLayer { get; set; }
}
```

**使用场景**: 武器、子弹、敌人攻击

**3. HurtboxComponent (受击判定)** - 继承 Area2D

```csharp
public partial class HurtboxComponent : Area2D
{
    private HealthComponent? _health;

    private void OnHitboxEntered(Area2D hitbox)
    {
        if (hitbox is HitboxComponent hb)
        {
            _health?.TakeDamage(hb.Damage);
        }
    }
}
```

**4. VelocityComponent (移动组件)**

```csharp
public partial class VelocityComponent : Node
{
    [Export] public float MaxSpeed { get; set; } = 300;
    [Export] public float Acceleration { get; set; } = 1000;
    [Export] public float Friction { get; set; } = 500;

    public Vector2 Velocity { get; set; }

    public void MoveToward(Vector2 direction, float delta) { }
    public void ApplyFriction(float delta) { }
}
```

**5. AttributeComponent (属性组件)**

```csharp
public partial class AttributeComponent : Node
{
    // 基础属性
    [Export] public float BaseDamage { get; set; }
    [Export] public float BaseSpeed { get; set; }
    [Export] public float CritChance { get; set; }

    // 属性修改器系统
    private List<StatModifier> _modifiers = new();

    public float GetFinalDamage() { /* 计算所有修改器 */ }
}
```

**6. FollowComponent (跟随组件)**

```csharp
public partial class FollowComponent : Node
{
    [Export] public Node2D? Target { get; set; }
    [Export] public float FollowSpeed { get; set; } = 200;

    public Vector2 GetDirectionToTarget() { }
}
```

**7. PickupComponent (拾取组件)** - 继承 Area2D

```csharp
using System;

public partial class PickupComponent : Area2D
{
    [Export] public float PickupRadius { get; set; } = 50;
    [Export] public float MagnetSpeed { get; set; } = 300;

    // ✅ 使用 C# Event
    public event Action<Node>? PickedUp;  // 参数：拾取者

    public override void _Ready()
    {
        AreaEntered += OnAreaEntered;
    }

    private void OnAreaEntered(Area2D area)
    {
        // 检测到玩家
        if (area.Owner is Player player)
        {
            PickedUp?.Invoke(player);
        }
    }
}
```

**实现位置**: `scenes/components/`
**预计工作量**: 3-5 天

---

### 🔲 3.2 Resource System (资源数据系统)

**优先级**: ⭐⭐⭐⭐ (高)

**职责**: 定义游戏静态配置，实现数据驱动设计

#### 必需资源类

**1. WeaponData (武器数据)**

```csharp
[GlobalClass]
public partial class WeaponData : Resource
{
    [Export] public string WeaponName { get; set; } = "";
    [Export] public Texture2D Icon { get; set; }
    [Export] public float Damage { get; set; } = 10;
    [Export] public float Cooldown { get; set; } = 1.0f;
    [Export] public float Range { get; set; } = 300;
    [Export] public int ProjectileCount { get; set; } = 1;
    [Export] public PackedScene ProjectileScene { get; set; }
}
```

**2. EnemyData (敌人数据)**

```csharp
[GlobalClass]
public partial class EnemyData : Resource
{
    [Export] public string EnemyName { get; set; } = "";
    [Export] public float MaxHp { get; set; } = 50;
    [Export] public float Speed { get; set; } = 100;
    [Export] public float Damage { get; set; } = 10;
    [Export] public int ExpReward { get; set; } = 5;
    [Export] public PackedScene EnemyScene { get; set; }
}
```

**3. WaveData (波次数据)**

```csharp
[GlobalClass]
public partial class WaveData : Resource
{
    [Export] public int WaveNumber { get; set; }
    [Export] public float Duration { get; set; } = 60;
    [Export] public Godot.Collections.Array<EnemySpawnConfig> Spawns { get; set; }
}

public partial class EnemySpawnConfig : Resource
{
    [Export] public EnemyData EnemyData { get; set; }
    [Export] public int Count { get; set; }
    [Export] public float SpawnInterval { get; set; }
}
```

**4. UpgradeData (升级数据)**

```csharp
[GlobalClass]
public partial class UpgradeData : Resource
{
    [Export] public string UpgradeName { get; set; } = "";
    [Export] public string Description { get; set; } = "";
    [Export] public Texture2D Icon { get; set; }
    [Export] public UpgradeType Type { get; set; }
    [Export] public float Value { get; set; }
}

public enum UpgradeType
{
    IncreaseDamage,
    IncreaseSpeed,
    IncreaseMaxHp,
    AddWeapon
}
```

**实现位置**:

- 类定义: `Resources/data/`
- 数据文件: `Resources/weapons/`, `Resources/enemies/`, `Resources/waves/`

**预计工作量**: 2-3 天

---

### 🔲 3.3 Event System (事件系统)

**优先级**: ⭐⭐⭐⭐⭐ (最高)

**职责**: 全局事件总线，解耦模块间通信

**核心原则**: **优先使用 C# 原生 Event，仅在必要时使用 Godot Signal**

#### 为什么优先用 C# Event？

| 特性         | C# Event              | Godot Signal              |
| :----------- | :-------------------- | :------------------------ |
| **性能**     | ✅ 极快（编译期优化） | ❌ 较慢（运行时反射）     |
| **类型安全** | ✅ 编译期检查         | ❌ 运行时检查             |
| **依赖性**   | ✅ 纯 C# 可用         | ❌ 必须继承 Node          |
| **适用场景** | ✅ 内部架构、逻辑解耦 | ⚠️ UI 连线、GDScript 交互 |

#### 实现方案：静态事件总线（推荐）

```csharp
// scenes/autoload/EventBus.cs
using System;
using Godot;

/// <summary>
/// 全局事件总线 - 使用 C# 原生 Event
/// 无需继承 Node，可以是纯静态类
/// </summary>
public static class EventBus
{
    // === 战斗事件 ===
    public static event Action<Node, Vector2>? EnemyDied;      // 参数：敌人节点, 位置
    public static event Action<float>? PlayerDamaged;          // 参数：伤害值
    public static event Action<float>? PlayerHealed;           // 参数：治疗量

    // === 波次事件 ===
    public static event Action<int>? WaveStarted;              // 参数：波次编号
    public static event Action<int>? WaveCompleted;            // 参数：波次编号

    // === 经验/升级事件 ===
    public static event Action<int>? ExperienceGained;         // 参数：经验值
    public static event Action<int>? LevelUp;                  // 参数：新等级
    public static event Action<UpgradeData>? UpgradeSelected;  // 参数：升级数据

    // === 游戏状态事件 ===
    public static event Action? GamePaused;
    public static event Action? GameResumed;
    public static event Action<bool>? GameOver;                // 参数：是否胜利

    // === 便捷触发方法（可选） ===
    public static void TriggerEnemyDied(Node enemy, Vector2 position)
    {
        EnemyDied?.Invoke(enemy, position);
    }

    public static void TriggerPlayerDamaged(float damage)
    {
        PlayerDamaged?.Invoke(damage);
    }

    // ... 其他触发方法
}
```

#### 使用示例

**发布事件**:

```csharp
// 方式1：直接触发
EventBus.EnemyDied?.Invoke(enemy, enemy.GlobalPosition);

// 方式2：使用便捷方法
EventBus.TriggerEnemyDied(enemy, enemy.GlobalPosition);
```

**订阅事件**:

```csharp
public partial class WaveManager : Node
{
    public override void _Ready()
    {
        // 订阅
        EventBus.EnemyDied += OnEnemyDied;
    }

    private void OnEnemyDied(Node enemy, Vector2 position)
    {
        GD.Print($"敌人死亡: {enemy.Name} at {position}");
        // 生成掉落物
    }

    // ⚠️ 重要：必须解绑，否则内存泄漏
    public override void _ExitTree()
    {
        EventBus.EnemyDied -= OnEnemyDied;
    }
}
```

#### 什么时候必须用 Godot Signal？

仅在以下 3 种情况使用 `[Signal]`：

1. **编辑器连线**: 需要在 Godot 编辑器中可视化连接信号
2. **GDScript 交互**: 如果有 GDScript 代码需要监听
3. **引擎内置信号**: 如 `Button.Pressed`、`Area2D.BodyEntered`

**混合方案示例**（如果需要同时支持 C# 和 GDScript）:

```csharp
public partial class Events : Node
{
    public static Events Instance { get; private set; }

    // C# Event（内部使用）
    public static event Action<int>? LevelUpEvent;

    // Godot Signal（GDScript 使用）
    [Signal] public delegate void LevelUpEventHandler(int newLevel);

    public override void _Ready()
    {
        Instance = this;

        // 桥接：C# Event 触发时同时发射 Signal
        LevelUpEvent += (level) => {
            EmitSignal(SignalName.LevelUp, level);
        };
    }

    public static void TriggerLevelUp(int level)
    {
        LevelUpEvent?.Invoke(level);
    }
}
```

**实现位置**: `scenes/autoload/EventBus.cs` (可选配置为 AutoLoad)
**预计工作量**: 0.5 天

---

### 🔲 3.4 Manager System (管理器系统)

**优先级**: ⭐⭐⭐⭐ (高)

**职责**: 统筹游戏各子系统，提供全局访问接口

#### 必需管理器

**1. GameManager (游戏管理器)** - AutoLoad

```csharp
public partial class GameManager : Node
{
    public static GameManager Instance { get; private set; }

    public enum GameState { Menu, Playing, Paused, GameOver }
    public GameState CurrentState { get; private set; }

    public int CurrentWave { get; private set; }
    public float GameTime { get; private set; }

    public void PauseGame() { }
    public void ResumeGame() { }
    public void RestartGame() { }
    public void LoadScene(string scenePath) { }
}
```

**2. WaveManager (波次管理器)**

```csharp
public partial class WaveManager : Node
{
    [Export] public Godot.Collections.Array<WaveData> Waves { get; set; }

    private int _currentWaveIndex = 0;
    private float _waveTimer = 0;
    private ObjectPool<Enemy> _enemyPool;

    public void StartWave(int waveIndex) { }
    public void SpawnEnemy(EnemyData data, Vector2 position) { }

    public override void _Process(double delta)
    {
        // 定时生成敌人
    }
}
```

**3. UpgradeManager (升级管理器)**

```csharp
public partial class UpgradeManager : Node
{
    [Export] public Godot.Collections.Array<UpgradeData> AllUpgrades { get; set; }

    public List<UpgradeData> GetRandomUpgrades(int count = 3)
    {
        // 随机选择升级选项
    }

    public void ApplyUpgrade(UpgradeData upgrade, Node target)
    {
        // 应用升级效果到目标
        switch (upgrade.Type)
        {
            case UpgradeType.IncreaseDamage:
                target.GetData().Add("Damage", upgrade.Value);
                break;
            // ...
        }
    }
}
```

**4. ResourceManager (资源管理器)** - AutoLoad

```csharp
public partial class ResourceManager : Node
{
    public static ResourceManager Instance { get; private set; }

    private Dictionary<string, Resource> _cache = new();

    public T Load<T>(string path) where T : Resource
    {
        if (_cache.TryGetValue(path, out var cached))
            return (T)cached;

        var resource = GD.Load<T>(path);
        _cache[path] = resource;
        return resource;
    }

    public void PreloadResources(string[] paths) { }
}
```

**实现位置**:

- AutoLoad: `scenes/autoload/GameManager.cs`, `scenes/autoload/ResourceManager.cs`
- 场景管理器: `scenes/managers/`

**预计工作量**: 3-4 天

---

### 🔲 3.5 System Layer (系统层)

**优先级**: ⭐⭐⭐ (中)

**职责**: 实现 ECS 风格的批量处理逻辑

**1. DamageSystem (伤害系统)** - AutoLoad

```csharp
public partial class DamageSystem : Node
{
    public static DamageSystem Instance { get; private set; }

    public float CalculateDamage(float baseDamage, Node attacker, Node target)
    {
        float finalDamage = baseDamage;

        // 暴击计算
        if (attacker.HasData())
        {
            float critChance = attacker.GetData().Get<float>("CritChance", 0);
            float critMultiplier = attacker.GetData().Get<float>("CritMultiplier", 2.0f);

            if (GD.Randf() < critChance)
            {
                finalDamage *= critMultiplier;
                ShowCritEffect(target);
            }
        }

        // 伤害减免
        if (target.HasData())
        {
            float damageReduction = target.GetData().Get<float>("DamageReduction", 0);
            finalDamage *= (1 - damageReduction);
        }

        return finalDamage;
    }

    private void ShowCritEffect(Node target) { }
}
```

**2. EffectSystem (特效系统)** - AutoLoad

```csharp
public partial class EffectSystem : Node
{
    public static EffectSystem Instance { get; private set; }

    private ObjectPool<GpuParticles2D> _particlePool;

    public void PlayEffect(string effectName, Vector2 position)
    {
        var particle = _particlePool.Get();
        particle.GlobalPosition = position;
        particle.Emitting = true;

        // 自动归还
        GetTree().CreateTimer(particle.Lifetime).Timeout += () => {
            ObjectPoolManager.ReturnToPool(particle);
        };
    }

    public void PlaySound(string soundPath) { }
    public void ShakeScreen(float intensity, float duration) { }
}
```

**实现位置**: `scenes/autoload/` 或 `scenes/systems/`
**预计工作量**: 2-3 天

---

## 4. 目录结构

```text
res://
├── assets/                     # 美术资源
│   ├── sprites/
│   │   ├── player/
│   │   ├── enemies/
│   │   └── weapons/
│   ├── sfx/                    # 音效
│   └── fonts/
│
├── scenes/
│   ├── autoload/               # AutoLoad 单例
│   │   ├── Events.cs           # ✅ 待实现
│   │   ├── GameManager.cs      # ✅ 待实现
│   │   ├── ResourceManager.cs  # ✅ 待实现
│   │   ├── DamageSystem.cs     # ✅ 待实现
│   │   └── EffectSystem.cs     # ✅ 待实现
│   │
│   ├── Tools/                  # 工具类
│   │   ├── Logger/
│   │   │   └── Log.cs          # ✅ 已完成
│   │   ├── ObjectPool/
│   │   │   └── ObjectPool.cs   # ✅ 已完成
│   │   ├── data/
│   │   │   └── Data.cs         # ✅ 已完成
│   │   └── extensions/
│   │       └── NodeExtensions.cs # ✅ 已完成
│   │
│   ├── components/             # 可复用组件
│   │   ├── HealthComponent.cs          # ✅ 待实现
│   │   ├── health_component.tscn
│   │   ├── HitboxComponent.cs          # ✅ 待实现
│   │   ├── HurtboxComponent.cs         # ✅ 待实现
│   │   ├── VelocityComponent.cs        # ✅ 待实现
│   │   ├── AttributeComponent.cs           # ✅ 待实现
│   │   ├── FollowComponent.cs          # ✅ 待实现
│   │   └── PickupComponent.cs          # ✅ 待实现
│   │
│   ├── entities/               # 游戏实体
│   │   ├── player/
│   │   │   ├── player.tscn
│   │   │   └── Player.cs
│   │   ├── enemies/
│   │   │   ├── BaseEnemy.cs
│   │   │   ├── slime.tscn
│   │   │   └── goblin.tscn
│   │   ├── projectiles/
│   │   │   ├── bullet.tscn
│   │   │   └── Bullet.cs
│   │   └── pickups/
│   │       ├── experience.tscn
│   │       └── Experience.cs
│   │
│   ├── managers/               # 场景管理器
│   │   ├── WaveManager.cs      # ✅ 待实现
│   │   ├── UpgradeManager.cs   # ✅ 待实现
│   │   └── ArenaManager.cs
│   │
│   ├── ui/                     # 用户界面
│   │   ├── hud/
│   │   │   ├── hud.tscn
│   │   │   └── HUD.cs
│   │   └── menus/
│   │       ├── main_menu.tscn
│   │       └── upgrade_menu.tscn
│   │
│   └── Test/                   # 测试场景
│       ├── Tools/
│       └── Test/
│
└── Resources/                  # 数据资源
    ├── data/                   # Resource 类定义
    │   ├── WeaponData.cs       # ✅ 待实现
    │   ├── EnemyData.cs        # ✅ 待实现
    │   ├── WaveData.cs         # ✅ 待实现
    │   └── UpgradeData.cs      # ✅ 待实现
    │
    ├── weapons/                # 武器配置 .tres
    │   ├── pistol.tres
    │   └── shotgun.tres
    │
    ├── enemies/                # 敌人配置 .tres
    │   ├── slime.tres
    │   └── goblin.tres
    │
    ├── waves/                  # 波次配置 .tres
    │   ├── wave_1.tres
    │   └── wave_2.tres
    │
    └── upgrades/               # 升级配置 .tres
        ├── damage_up.tres
        └── speed_up.tres
```

---

## 5. 设计模式与最佳实践

### 5.1 伪 ECS 核心模式

#### Entity (实体) = Node + Components + Data

**传统 OOP 方式（❌ 不推荐）**:

```csharp
public partial class Enemy : CharacterBody2D
{
    private float _maxHp = 100;
    private float _currentHp = 100;
    private float _speed = 200;
    private Vector2 _velocity;

    public override void _PhysicsProcess(double delta)
    {
        // 移动逻辑
        // 战斗逻辑
        // AI 逻辑
        // ... 所有逻辑混在一起，难以复用
    }
}
```

**伪 ECS 方式（✅ 推荐）**:

```csharp
public partial class Enemy : CharacterBody2D
{
    // 组件引用
    private HealthComponent _health;
    private VelocityComponent _velocity;
    private FollowComponent _follow;
    private HurtboxComponent _hurtbox;

    // 动态数据
    private Data _data;

    // 静态配置
    [Export] public EnemyData EnemyData { get; set; }

    public override void _Ready()
    {
        // 获取组件
        _health = GetNode<HealthComponent>("HealthComponent");
        _velocity = GetNode<VelocityComponent>("VelocityComponent");
        _follow = GetNode<FollowComponent>("FollowComponent");
        _hurtbox = GetNode<HurtboxComponent>("Hurtbox");

        // 初始化动态数据
        _data = this.GetData();
        _data.Set("IsBoss", EnemyData.IsBoss);
        _data.Set("Reward", EnemyData.ExpReward);

        // 从配置初始化组件
        _health.MaxHp = EnemyData.MaxHp;
        _velocity.MaxSpeed = EnemyData.Speed;

        // 监听事件
        _health.Died += OnDied;
    }

    public override void _PhysicsProcess(double delta)
    {
        // 简洁的逻辑，委托给组件
        var direction = _follow.GetDirectionToTarget();
        _velocity.MoveToward(direction, (float)delta);
        Velocity = _velocity.Velocity;
        MoveAndSlide();
    }

    private void OnDied()
    {
        // ✅ 使用 C# Event 触发全局事件
        EventBus.TriggerEnemyDied(this, GlobalPosition);
        ObjectPoolManager.ReturnToPool(this);
    }
}
```

**优势**:

- ✅ 组件可复用（玩家和敌人共享 HealthComponent）
- ✅ 逻辑清晰，易于维护
- ✅ 便于测试（可单独测试组件）
- ✅ 支持可视化编辑（在编辑器中添加/移除组件）

---

### 5.2 组件通信模式

**核心原则**: **优先使用 C# Event，避免过度使用 Godot Signal**

#### 方式 1: C# Event - 推荐用于组件间通信（⭐⭐⭐⭐⭐）

```csharp
using System;

// HealthComponent 定义事件
public partial class HealthComponent : Node
{
    // ✅ 使用 C# Event
    public event Action? Died;
    public event Action<float>? Damaged;

    public void TakeDamage(float damage)
    {
        CurrentHp -= damage;
        Damaged?.Invoke(damage);  // 触发事件

        if (CurrentHp <= 0)
        {
            Died?.Invoke();
        }
    }
}

// Enemy 监听事件
public partial class Enemy : CharacterBody2D
{
    private HealthComponent _health;

    public override void _Ready()
    {
        _health = GetNode<HealthComponent>("HealthComponent");

        // ✅ 订阅事件
        _health.Died += OnDied;
        _health.Damaged += OnDamaged;
    }

    private void OnDied()
    {
        GD.Print("敌人死亡");
        EventBus.TriggerEnemyDied(this, GlobalPosition);
    }

    private void OnDamaged(float damage)
    {
        GD.Print($"受到伤害: {damage}");
    }

    // ⚠️ 重要：必须解绑，防止内存泄漏
    public override void _ExitTree()
    {
        if (_health != null)
        {
            _health.Died -= OnDied;
            _health.Damaged -= OnDamaged;
        }
    }
}
```

**优势**:

- ✅ 性能最好（编译期优化）
- ✅ 类型安全（编译期检查）
- ✅ 代码简洁
- ✅ 支持 Lambda 表达式

**注意事项**:

- ⚠️ 必须在 `_ExitTree()` 中解绑，否则内存泄漏
- ⚠️ 使用 `?.Invoke()` 防止空引用

---

### 5.3 数据管理模式

#### 静态配置 (Resource) vs 动态数据 (Data)

**静态配置 (Resource)** - 用于设计时确定的数据

```csharp
// WeaponData.tres
[Export] public float Damage { get; set; } = 10;  // 基础伤害
[Export] public float Cooldown { get; set; } = 1.0f;
```

**动态数据 (Data)** - 用于运行时变化的数据

```csharp
// 运行时修改
player.GetData().Set("CurrentDamage", 10);
player.GetData().Add("CurrentDamage", 5);  // Buff 加成
```

**组合使用**:

```csharp
public partial class Weapon : Node2D
{
    [Export] public WeaponData WeaponData { get; set; }  // 静态配置

    public override void _Ready()
    {
        // 将静态配置复制到动态数据
        var data = this.GetData();
        data.Set("BaseDamage", WeaponData.Damage);
        data.Set("CurrentDamage", WeaponData.Damage);
        data.Set("Cooldown", WeaponData.Cooldown);
    }

    public float GetFinalDamage()
    {
        // 从动态数据读取（包含 Buff 加成）
        return this.GetData().Get<float>("CurrentDamage");
    }
}
```

---

### 5.4 对象池使用模式

#### 模式 1: 预创建池（推荐）

```csharp
// 在场景初始化时创建
public partial class Main : Node
{
    private ObjectPool<Bullet> _bulletPool;
    private ObjectPool<Enemy> _enemyPool;

    public override void _Ready()
    {
        var bulletScene = GD.Load<PackedScene>("res://Src/entities/projectiles/bullet.tscn");
        _bulletPool = new ObjectPool<Bullet>(
            () => bulletScene.Instantiate<Bullet>(),
            new ObjectPoolConfig {
                InitialSize = 100,  // 预热
                MaxSize = 500,
                Name = "BulletPool"
            }
        );
    }
}
```

#### 模式 2: 实现 IPoolable 接口

```csharp
public partial class Bullet : CharacterBody2D, IPoolable
{
    public void OnPoolAcquire()
    {
        // 对象被取出时调用
        Visible = true;
        ProcessMode = ProcessModeEnum.Inherit;
        // 重置状态
        Velocity = Vector2.Zero;
    }

    public void OnPoolRelease()
    {
        // 对象被归还时调用
        // 清理引用，停止特效
        GetNode<GpuParticles2D>("Trail").Emitting = false;
    }

    public void OnPoolReset()
    {
        // 重置数据
        this.GetData().Clear();
    }
}
```

#### 模式 3: 自动归还

```csharp
public partial class Bullet : CharacterBody2D
{
    private float _lifetime = 5.0f;
    private float _timer = 0;

    public override void _Process(double delta)
    {
        _timer += (float)delta;
        if (_timer >= _lifetime)
        {
            // 自动归还到池
            ObjectPoolManager.ReturnToPool(this);
        }
    }
}
```

---

### 5.5 性能优化要点

#### 1. 避免频繁的 GetNode 调用

**❌ 不好的做法**:

```csharp
public override void _Process(double delta)
{
    GetNode<HealthComponent>("Health").TakeDamage(1);  // 每帧查找
}
```

**✅ 好的做法**:

```csharp
private HealthComponent _health;

public override void _Ready()
{
    _health = GetNode<HealthComponent>("Health");  // 只查找一次
}

public override void _Process(double delta)
{
    _health.TakeDamage(1);
}
```

#### 2. 使用对象池

**必须使用对象池的场景**:

- 子弹（生命周期 < 5 秒）
- 敌人（频繁生成/销毁）
- 掉落物
- 粒子特效节点
- UI 伤害数字

#### 3. 批量处理

```csharp
// ❌ 逐个处理
foreach (var enemy in enemies)
{
    enemy.TakeDamage(10);
}

// ✅ 批量处理（如果可能）
DamageSystem.Instance.ApplyAreaDamage(enemies, 10, position, radius);
```

#### 4. 使用 Data 而非 Metadata

**❌ Godot 的 Metadata（性能较差）**:

```csharp
node.SetMeta("IsBoss", true);
var isBoss = (bool)node.GetMeta("IsBoss");
```

**✅ 使用 Data 容器（性能更好）**:

```csharp
node.GetData().Set("IsBoss", true);
var isBoss = node.GetData().Get<bool>("IsBoss");
```

---

## 6. 开发路线图

### 阶段 1: 核心框架完善（1-2 周）

**目标**: 完成伪 ECS 框架的核心系统

#### Week 1: 组件系统 + 事件系统

- [ ] **Day 1-2**: HealthComponent + HitboxComponent + HurtboxComponent
  - 实现基础战斗逻辑
  - 编写单元测试
- [ ] **Day 3**: VelocityComponent + FollowComponent
  - 实现移动和 AI 基础
- [ ] **Day 4**: AttributeComponent + PickupComponent
  - 实现属性系统和拾取逻辑
- [ ] **Day 5**: Events 事件系统
  - 定义所有核心事件
  - 集成到现有组件

#### Week 2: 资源系统 + 管理器

- [ ] **Day 1-2**: Resource 数据类
  - WeaponData, EnemyData, WaveData, UpgradeData
  - 创建示例 .tres 文件
- [ ] **Day 3**: GameManager + ResourceManager
  - 游戏状态管理
  - 资源预加载
- [ ] **Day 4**: WaveManager + UpgradeManager
  - 波次生成逻辑
  - 升级系统
- [ ] **Day 5**: DamageSystem + EffectSystem
  - 伤害计算
  - 特效管理

---

### 阶段 2: 核心玩法原型（1-2 周）

**目标**: 实现可玩的最小原型

#### Week 3: 玩家 + 敌人 + 战斗

- [ ] **Day 1-2**: 玩家实体
  - 移动控制
  - 基础武器（手枪）
  - 血量显示
- [ ] **Day 3-4**: 敌人实体
  - 2-3 种基础敌人
  - AI 跟随逻辑
  - 对象池集成
- [ ] **Day 5**: 战斗系统整合
  - Hitbox/Hurtbox 交互
  - 伤害计算
  - 死亡处理

#### Week 4: 波次 + 升级 + UI

- [ ] **Day 1-2**: 波次系统
  - 定时生成敌人
  - 波次配置
  - 难度递增
- [ ] **Day 3-4**: 升级系统
  - 经验值和等级
  - 升级选择 UI
  - 属性应用
- [ ] **Day 5**: HUD + 游戏循环
  - 血量/经验条
  - 波次计时器
  - 胜利/失败判定

---

### 阶段 3: 内容扩展（2-4 周）

**目标**: 丰富游戏内容

- [ ] 添加 5-10 种武器
- [ ] 添加 10-15 种敌人
- [ ] 添加 20-30 种升级
- [ ] 添加 Boss 战
- [ ] 添加特殊事件
- [ ] 音效和粒子特效

---

### 阶段 4: 优化与打磨（1-2 周）

**目标**: 性能优化和体验优化

- [ ] 性能分析（Profiler）
- [ ] 对象池优化
- [ ] 碰撞检测优化
- [ ] UI/UX 优化
- [ ] 平衡性调整
- [ ] Bug 修复

---

## 7. 关键技术决策

### 7.1 为什么使用伪 ECS 而非真 ECS？

**真 ECS 的问题**:

- ❌ 与 Godot 编辑器集成困难
- ❌ 无法使用可视化场景编辑
- ❌ 学习曲线陡峭
- ❌ 调试困难

**伪 ECS 的优势**:

- ✅ 保留 Godot 编辑器的便利性
- ✅ 组件可视化编辑
- ✅ 易于理解和维护
- ✅ 性能足够（500+ 实体无压力）

### 7.2 为什么需要 Data 容器？

**Godot Metadata 的问题**:

- ❌ 性能较差（基于 Variant）
- ❌ 无类型安全
- ❌ 无事件监听

**Data 容器的优势**:

- ✅ 强类型访问
- ✅ 支持算术运算（Add/Multiply）
- ✅ 事件监听
- ✅ 更好的性能

### 7.3 为什么需要对象池？

**性能数据**（基于测试）:

- 无对象池: 500 敌人 + 1000 子弹 = 30 FPS
- 有对象池: 500 敌人 + 1000 子弹 = 60 FPS

**关键原因**:

- 避免频繁 GC（垃圾回收）
- 减少内存分配
- 提升帧率稳定性

---

## 8. 常见问题 (FAQ)

### Q1: 组件之间如何通信？

**A**: **优先使用 C# Event**，其次是直接调用。避免组件间直接引用。

**推荐顺序**:

1. C# Event（组件间通信）⭐⭐⭐⭐⭐
2. 直接调用（父子关系）⭐⭐⭐⭐
3. 全局事件总线（跨场景）⭐⭐⭐⭐
4. Godot Signal（仅编辑器连线）⭐⭐

### Q2: 什么时候使用 Resource，什么时候使用 Data？

**A**:

- Resource: 设计时确定的静态配置（武器基础伤害）
- Data: 运行时变化的动态数据（当前伤害 = 基础伤害 + Buff）

### Q3: 对象池的 InitialSize 和 MaxSize 如何设置？

**A**:

- InitialSize: 场景平均使用量（如子弹池设为 50-100）
- MaxSize: 峰值使用量的 1.5-2 倍（如子弹池设为 200-500）

### Q4: 如何调试组件系统？

**A**:

1. 使用 Log 工具记录关键事件
2. 在编辑器中查看组件状态（Export 变量）
3. 使用 Godot Debugger 的 Remote 面板

### Q5: 性能瓶颈在哪里？

**A**:

1. 碰撞检测（使用合理的 CollisionLayer）
2. 频繁实例化（使用对象池）
3. 过多的 GetNode 调用（缓存引用）

---

## 9. 参考资源

### 官方文档

- [Godot C# 文档](https://docs.godotengine.org/en/stable/tutorials/scripting/c_sharp/index.html)
- [Godot Signals](https://docs.godotengine.org/en/stable/getting_started/step_by_step/signals.html)

### 推荐阅读

- [ECS 架构介绍](https://github.com/SanderMertens/ecs-faq)
- [Godot 性能优化指南](https://docs.godotengine.org/en/stable/tutorials/performance/index.html)

### 类似项目参考

- [Vampire Survivors](https://store.steampowered.com/app/1794680/Vampire_Survivors/)
- [Brotato](https://store.steampowered.com/app/1942280/Brotato/)

---

## 10. 总结

### 已完成 ✅

1. **Log** - 日志系统
2. **ObjectPool** - 对象池
3. **Data** - 动态数据容器
4. **NodeExtensions** - 节点扩展

### 待实现 🔲

1. **Component System** - 7 个核心组件（优先级最高）
2. **Event System** - 全局事件总线（优先级最高）
3. **Resource System** - 4 个数据类
4. **Manager System** - 4 个管理器
5. **System Layer** - 2 个系统

### 预计总工作量

- 核心框架: 2 周
- 玩法原型: 2 周
- 内容扩展: 2-4 周
- 优化打磨: 1-2 周
- **总计: 7-10 周**

### 下一步行动

1. 实现 HealthComponent（最基础的组件）
2. 实现 Events 事件系统（解耦通信）
3. 创建简单的玩家和敌人原型
4. 测试战斗循环

---

**文档版本**: v1.0
**最后更新**: 2024-12-21
**维护者**: 项目团队
