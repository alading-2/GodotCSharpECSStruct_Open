
## 问题

## Data
更新文档，检查

## Now

## 碰撞，包含移动系统碰撞逻辑
- 碰撞问题：现在是在PlayerEntity、EnemyEntity这些CharacterBody2D上设置layer,mask,collision，layer,mask没问题，但是collision问题来了，现在角色是通过序列帧转成AnimatedSprite2D，然后挂载到PlayerEntity、EnemyEntity这些单位Entity上面，但是每个单位AnimatedSprite2D的碰撞形状是不一样的，但是CharacterBody2D上面的形状是统一的。
  我这么想，PlayerEntity、EnemyEntity不设置碰撞，而是另外新建一个场景PlayerCollision、EnemyCollision，是CharacterBody2D类型，但不设置碰撞形状，而是将PlayerCollision、EnemyCollision实例化到对应的AnimatedSprite2D下面，再新增形状，这样每个CharacterBody2D都可以设置适合的CollisionShape2D。
  而且PlayerCollision、EnemyCollision我一改，实例化的地方全部改了
  还有个问题就是PlayerCollision、EnemyCollision碰撞统一设置应该放在ECS框架的哪个位置？放Src/路径还是放Data/路径？
  所有碰撞相关的组件都应该使用这个碰撞而不是再去用PlayerEntity、EnemyEntity的碰撞，PlayerEntity、EnemyEntity的碰撞已经不用了。
  这是CharacterBody2D，Area2D一样，比如后面要做的EffectEntity，WeaponEntity都是Area2D，都这样用。
  也就是Entity不做碰撞，碰撞放到AnimatedSprite2D上，但又有一个问题，AnimatedSprite2D是序列帧动画，如果只有一张图片，比如WeaponEntity很可能就一张图片，那么这个适合就是Sprite2D了，碰撞就加到这个Sprite2D上。
- 然后这个过程肯定不是手动加上去的，要用SpriteFramesGenerator，SpriteFramesGenerator暂时没有增加碰撞实例化场景，名字统一为Collision，sprite_frames_config.gd新加配置：比如res://assets/Effect路径我就用EffectCollision场景。
- SpriteFramesGenerator的配置管理很分散，我认为应该统一一个字典设置，比如key=Effect，value=一个字典newDict，newDict.路径=res://assets/Effect，newDict.统一动画命名=effect，newDict.碰撞场景=对应碰撞的场景路径，如果没有统一路径用NAME_MAP。BATCH_PATHS只扫描序列帧不扫码但单个图，如果这个统一字典里面的路径也要处理，相当于加到批量扫描路径后得到的需要处理的路径里面，如果有单张照片，则生成Sprite2D场景。然后将newDict.碰撞场景实例化到当前场景中
---
- MovementParams.DestroyOnCollision没有完成，与单位碰撞时是否自动销毁实体的逻辑，这时候要改SpriteFramesGenerator，因为现在序列帧转场景没有特定的碰撞，碰撞形状到时候自定义就是了，但是碰撞层级可以预先设置，比如assets\Unit\Enemy中敌人的mask和plyer都是一样的，我想在sprite_frames_config.gd配置文件中用自动设置，比如assets\Unit\Enemy路径下的player、mask都设置成这样，必须支持一键批量设置
  加个移动系统onCollision碰撞时执行

- [] 伤害是选择工具遍历entity距离，还是用碰撞事件？特效、武器、子弹都要一般都要用碰撞。

- SpriteFramesGenerator我做了只能更新功能，现在要支持碰撞形状不要变，不然我一更新全变了那就麻烦了
- Tools\ResourceGenerator路径问题，这样的变量+路径太重了，还是改会之前的直接名字就行，当然之前应该是怕重名，那就分析是哪些重名，怎么避免

- 另外一种思路，碰撞事件可通用，根据发生碰撞的layer,mask来判断是哪种碰撞。但是这样的话CollisionSensorComponent实例化之后还要手动设置layer,mask，可能会写错，我的想法是在Src\ECS\Component\Collision\Presets把受到伤害范围碰撞、拾取物品范围碰撞都写好，用一个枚举写好是什么碰撞，然后在CollisionSensorComponent Export导出一个枚举变量，选择对应的碰撞类型即可，这样就不用手动设置了，因为Src\ECS\Component\Collision\Presets里面把碰撞什么layer,mask都统一了，其他地方复制就行，
  也可以用脚本记录对应碰撞layer,mask的数值，然后赋值，不过我感觉直接在godot场景设置好一点，因为更直观，有检查器设置碰撞层更直接，最好是在godot场景中设置好了，脚本跟着改就好了

- EntityManager新加一个CollisionComponent.FindCollisionNode，在Entity中找视觉场景、在Entity中找视觉场景中的碰撞节点、碰撞事件中通过目标节点找到Entity


## 移动系统
- [x]新增贝塞尔、回旋镖运动效果

- **要不要用行为树状态机来管理运动系统，比如运动结束时自动切换成优先级高的一种状态？不对，运动跟状态是两个概念，比如眩晕的时候可以运动**
- **更新运动系统所有说明文档，尤其是一大堆<c></c>感觉不好，深度思考**

- Godot Tween是否能替代我现在的运动策略，为什么
  

## 伤害
- 碰撞后一次伤害/多次伤害，伤害间隔

## TodoList
- [x] 模板技能
- [x] Line2D检查

- [x] 详细查看技能逻辑
- [x] 技能增加参数：特效场景
- [ ] Line2D做成Entity？

- 动画问题，受到攻击动画播放时会覆盖攻击动画？还是说不是覆盖，而是在播放受到攻击动画的过程中，如果又触发了攻击动画，不会打断受到攻击动画，所以看不到攻击动画

- [] 技能逻辑复用，比如特效、伤害这种可以写成工具不用次次都重新
- 技能执行函数要改，现在只是针对技能的，但是要做成通用的Action，无论是什么触发都能执行，不制止只是技能，而且要支持多重分类，一层分类有技能等，技能下面还有分什么类型的技能

- [] 详细查看数字飘字逻辑

- [] DataKey检查，分多过多的分多一个文件
- [] 事件检查

- 表格里面不能填具体伤害，比如我想填技能伤害=防御力*3+10，这个应该怎么做？或者说这个应该算逻辑不是数据？



## 大方向
- []特效系统
- []物品系统
- []BUFF系统
- []三选一玩法

## 表格插件
- 改成C#？怎么显示中文注释，现在是用映射的方法，每次还要自己改，麻烦，我认为纯C#应该可以解决，考虑是否是用.tres
- 而且表格插件不能用继承，比如enemyconfig继承unitconfig，但是enemyconfig不能重置unitconfig的属性

## 完成基本Demo后有待完善
- []AI系统检查
- AI新加的东西检查，TargetSelector检查
- []Data事件、DataKey的规范写到Skill
- []ComponentPreset用代码代替场景是否更好？相关的Component放在一起，比如位置目标技能的右摇杆移动组件应该跟技能相关组件放一起
- []检查Datakey和Datakeyregister

## 有待解决
- Data系统、伤害系统增伤易伤区分
- 护盾、Buff、Effect系统
- Docs\ECS\修改\有待修改.md

- Tween 动画做成工具？有Timer有必要用Tween吗？






