## 问题

- \_resourceToPoolMap 自动推断池名称？EntityManager，Spawn 增加参数：池名称，才知道从哪个池获取实例
- 我不知道你在干什么？Objectpool 你是没看过吗？直接用 Get 不行吗？我知道问题在哪了，IObjectPool 没有写全 Objectpool 的方法，修改 Objectpool

```csharp
        // 使用反射调用泛型 Get 方法（性能考虑：可以缓存 MethodInfo）
        var poolType = pool.GetType();
        var getMethod = poolType.GetMethod("Get");
        if (getMethod == null)
        {
            _log.Error($"对象池 {poolName} 没有 Get 方法");
            return null;
        }

        var obj = getMethod.Invoke(pool, null);
        var entity = obj as T;
```

---

### 有待解决

- 生成 Enemy 自动更新数据，即 Entity 功能
- Docs\ECS\修改\有待修改.md

### VSCodeDebug

### 其他

- PropertyMap 名字可以思考怎么改，

```

```
