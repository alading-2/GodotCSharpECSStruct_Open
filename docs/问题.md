## 问题
### 伤害系统

### 1
- 更新文档

### 2
- 伤害计算流程处理物理、法术、真实伤害、

- LifestealProcessor是基于最终造成的伤害恢复生命值，

### 组件：生命周期、死亡复活、恢复、伤害重构
- 要使用EventBus事件系统
- Component的核心原则：功能单一
- 以资深游戏开发者结合现代游戏设计框架深度思考组件需要分成几个，每个组件完成什么功能，组件什么名字
- LifecycleComponent主要管理生命周期，死亡复活、限时存活的单位（召唤物）、击杀单位等，参考LifecycleComponent.ts
- 剩下根据生命值变化判定死亡、受伤、恢复。恢复单独函数触发恢复事件，根据生命值变化不触发恢复事件，因为恢复事件一般伴随特性、恢复数字效果。
- 恢复是用单独计时器每秒回血回蓝还是新建RecoverySystem统一管理每秒回血、回蓝，UnitDataRegister禁疗检测
- reset不需要，Entity归还对象池就清空data了
- 支持完全重构，需要生成新方案的说明文档


### 修改
- RecoveryComponent还是改成RecoveryComponent+RecoverySystem，OnComponentRegistered向RecoverySystem注册Entity，OnComponentUnregistered取消，监听DataKey_Attribute,DataKey中恢复相关的属性（最终生命魔法恢复），如果恢复属性>0则注册，添加是否注册的属性，RecoverySystem获取Entity的Data的恢复属性，如果<=0则注销属性，这样就能一个计时器完成整个恢复系统，而且只有恢复属性>0的才遍历，高效，如果RecoveryComponent单独一个计时器，大量单位就需要大量的计时器实例。要实现这些功能很简单，精简实现


- Data\Resources\Unit，Resource属性怎么设置Data更新
- Component数据初始化，不应该[Export]，统一由EntityManager.Spawn时初始化Component数据，而这个数据是Data\Resources\Unit里面的Resource属性获取，统一由EntityManager.LoadFromResource注入Entity的Data，而Component数据初始化应该从Data中获取，这样的话数据注入应该放在防止重
- 复注册之前


- 严重问题：事件系统死循环：LifecycleComponent 241-242
- 耦合问题严重，Component里面通过GetComponent
- HealthComponent的治疗事件只有一个，其他地方触发治疗事件，但治疗事件里面又要触发另外一个治疗事件，这样是用一个治疗事件搞定还是怎么整？

- 项目框架重构，只保留简单说明+相关说明文档的路径，支持Ctrl+左键跳转文档，这样就不用每改一个地方都要重新修改项目框架文档
- Data,Event,Entity,Component单独测试
- type、config放哪里

### DamageComponent
- HealthComponent的造成伤害放到DamageComponent
- DamageComponent，造成伤害的方式，伤害统计，造成伤害判断死亡






### 有待解决

- Spine图片转场景插件全局更改时缓存已经修改过位置的场景，不然又要手动修改

- 属性、伤害、Buff 系统
- Docs\ECS\修改\有待修改.md
- 显示图片做成 Component

---

### 其他

-

```

```


