
## 问题

## Data
更新文档，检查

## Now
- OnExit不如改成OnSwitch切换逻辑时执行
- 其他逻辑的注释例子加上ReachDistance


## 移动系统
- [] 技能引出运动模块，特效移动，我感觉组件好一点
  - 现在需要做运动功能，我偏向做成组件，放在EffectEntity上，让EffectEntity有运动能力，这是常见功能
  需要做的功能：向固定方向冲锋、冲锋追踪目标、环绕（目标/中心点，可以有向心力），反正需要详细分析做成什么功能，还可以扩展很多相关的运动模式
  先不生成代码，在Docs\框架\ECS\Component生成提示词文档
- 移动冲突，比如player移动、敌人移动和动作组件，一起运作会出问题，怎么解决？
- 移动组件把这么多移动逻辑放在一起真的对吗？感觉写法比较低级，有没有更好的方式？ECS系统可以Component+System结合
- 新增贝塞尔、回旋镖运动效果

- 移动系统让AI分析有无问题
- 运动系统所有策略必须在开头注释写明怎么用这个运动模式，要设置哪些DataKey，有什么效果，详细注释说名
- **要不要用行为树状态机来管理运动系统，比如运动结束时自动切换成优先级高的一种状态？不对，运动跟状态是两个概念，比如眩晕的时候可以运动**
- **更新运动系统所有说明文档，尤其是一大堆<c></c>感觉不好，深度思考**
- TODO MovementParams.DestroyOnCollision没有完成，与单位碰撞时是否自动销毁实体的逻辑，这时候要改SpriteFramesGenerator，因为现在序列帧转场景没有特定的碰撞，碰撞形状到时候自定义就是了，但是碰撞层级可以预先设置，比如assets\Unit\Enemy中敌人的mask和plyer都是一样的，我想在sprite_frames_config.gd配置文件中用自动设置，比如assets\Unit\Enemy路径下的player、mask都设置成这样，必须支持一键批量设置
  加个onCollision碰撞时执行
- 同时两种运动是否要支持？
- 攻击投射物也要用到移动系统，抛物线还是贝塞尔曲线？
- 上下限这种模式能否做成通用，比如环绕中的径向速度移动距离有上下限，那波形运动正弦波的振幅频率也可以有上下限。而且上下限还不够，比如我到了下限，就从降低变成升高，然后到上限，再到下限，这种移动模式也很常见
- 根据war3jass增加一些移动策略
- 移动系统根据IMovementStrategy加个OnEnd，比如回旋镖结束做什么，追踪敌人结束做什么，跟OnExit不同，OnExit是切换策略，可能是被强制切换的，比如最终敌人一般到某个范围内都算追上了，如果提前追上也要提前结束
  回旋镖一般不是回到开始点，最后回到player的最新位置，而不是旧位置
  回旋镖肯定回来，不回来还叫回旋镖吗？
  回旋镖逻辑很多问题，很不完善，太简陋了
  FinalMoveSpeed大错特错，用MaxDuration来设置什么时间完成
  其实回旋镖应该要传入更多的参数才能实现真正的回旋镖，现在这个简直是乱写的
  

## TodoList
- [x] 模板技能
- [x] Line2D检查

- [x] 详细查看技能逻辑
- [x] 技能增加参数：特效场景
- [ ] Line2D做成Entity？

- 动画问题，受到攻击动画播放时会覆盖攻击动画？还是说不是覆盖，而是在播放受到攻击动画的过程中，如果又触发了攻击动画，不会打断受到攻击动画，所以看不到攻击动画

- [] 技能逻辑复用，比如特效、伤害这种可以写成工具不用次次都重新写
- [] 伤害是选择工具遍历entity距离，还是碰撞？尤其是技能中特效运动的场景。但是如果后面要改这个范围，Data容易改，如果是碰撞能改吗？

- [] 详细查看数字飘字逻辑

- [] DataKey检查，分多过多的分多一个文件
- [] 事件检查

- 表格里面不能填具体伤害，比如我想填技能伤害=防御力*3+10，这个应该怎么做？或者说这个应该算逻辑不是数据？

## 大方向
- []特效系统
- []物品系统
- []BUFF系统
- []三选一玩法

## 表格插件
- 改成C#？怎么显示中文注释，现在是用映射的方法，每次还要自己改，麻烦，我认为纯C#应该可以解决，考虑是否是用.tres
- 而且表格插件不能用继承，比如enemyconfig继承unitconfig，但是enemyconfig不能重置unitconfig的属性

## 完成基本Demo后有待完善
- []AI系统检查
- AI新加的东西检查，TargetSelector检查
- []Data事件、DataKey的规范写到Skill
- []ComponentPreset用代码代替场景是否更好？相关的Component放在一起，比如位置目标技能的右摇杆移动组件应该跟技能相关组件放一起
- []检查Datakey和Datakeyregister

## 有待解决
- Data系统、伤害系统增伤易伤区分
- 护盾、Buff、Effect系统
- Docs\ECS\修改\有待修改.md

- Tween 动画做成工具？有Timer有必要用Tween吗？






