## 问题

- \_ruleTimerRoot
-
- SpawnSystem 重构，SpawnSystem 的主要功能是生成 Enemy(entity)，

  - SpawnSystem 包括了波数设置，每波多少秒，多少波，开始结束的设定
  - 4 种位置计算工具应该单独成一个 CS 文件

- SpawnSystem 默认是波数自动生成，要预留手动批量生成的方法，用于测试
- 为何需要\_ruleTimerRoot，
- ValidateAllPools、ValidatePoolExists 我认为是过度验证，深度思考
- 每秒生成一个的做法很粗糙，土豆兄弟有扎堆生成的，有分散生成的，EnemySpawnRule 应该加一个每次生成多少数量
- 最终目的是生成一个非常通用功能强大的 SpawnSystem，深度思考，还有很多地方没有没写好

### 有待解决

- ObjectPool 生成对象时的 parent 管理，比如 Enemy 放在 Tree/ECS/Entity/Player,Tree/ECS/Entity/Enemy,

- Docs\ECS\修改\有待修改.md

### 其他

- PropertyMap 名字可以思考怎么改，
